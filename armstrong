#include <stdio.h>
int main()
{
    int n, res= 0, remainder=0,i;
    scanf("%d", &n);
for(i=0;i<=n;i++)
    while(n != 0)
    {
        remainder = n%10;
        res =remainder*remainder*remainder ;
        n /= 10;
    }
    if(res==n)
    printf("armstrong");
    else
    printf("not");
    return 0;
}
